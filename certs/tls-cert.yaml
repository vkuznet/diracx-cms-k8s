apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: diracx-tls
  namespace: default
spec:
  secretName: diracx-tls-secret
  dnsNames:
    - example.local
  issuerRef:
    name: diracx-ca-issuer
